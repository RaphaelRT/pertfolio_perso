import Typed from './library/typed.js'
import './library/typed.min.js'

let typed = new Typed('.typed_h2', {
    strings:[
      "Web developper / Hetic Student"
    ],
    typeSpeed:80,
    backDelay:200,
    loop:false
  })